Para usar el servidor necesitamos:

	-Abrir la termina/cmd
	- Ir a la ruta de donde esta el proyecto (servidor)
	- Compilar los archivos si aun no estan compilados:
		javac -cp ".;la\ruta\va\aqui" Servidor.java
	-Ejecutar el servidor
		java -cp ".;C:\Users\JuanPablo\Documents\ProyectoRMI\DTOs" Servidor

Si el servidor corre correctamente se mostrará:
	"ServidorPagosRMI listo en el puerto 1100."

Si quiere cerrar el servidor darle control C

